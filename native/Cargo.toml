[workspace]
resolver = "2"
members = ["core", "napi"]

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
strip = "symbols"
# Keep unwinding (the default): the addon turns a Rust panic into a rejected
# promise instead of aborting the Node process.
panic = "unwind"
